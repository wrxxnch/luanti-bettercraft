name = bc_mineclonia_commands
description = Adds Minecraft-style commands (execute, particle, testfor) with relative coordinates and autocomplete.
depends = mcl_core
optional_depends = mcl_core, mcl_particles, mcl_mobs
